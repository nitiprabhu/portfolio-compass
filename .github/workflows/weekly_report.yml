name: Weekly Strategic Report

on:
  schedule:
    - cron: '30 3 * * 0' # 09:00 IST (03:30 UTC) Sunday
  workflow_dispatch:

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Weekly Report
        run: |
          curl -X POST https://portfolio-compass-k4aw.onrender.com/api/cron/weekly-report
