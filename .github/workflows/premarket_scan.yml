name: Pre-Market Gap Scan

on:
  schedule:
    - cron: '30 13 * * 1-5' # 19:00 IST (13:30 UTC) Mon-Fri
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Pre-Market Scan
        run: |
          curl -X POST https://portfolio-compass-k4aw.onrender.com/api/cron/premarket-scan
