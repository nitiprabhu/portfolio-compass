name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *' # Runs every 30 minutes
  workflow_dispatch: # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Portfolio Compass
        run: |
          curl -I https://portfolio-compass-k4aw.onrender.com/
